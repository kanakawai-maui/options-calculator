{
  "name": "ai-options",
  "private": true,
  "version": "1.0.0",
  "description": "React + Zustand options P/L heatmap with Yahoo Finance backend",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "npm --prefix server install && npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
